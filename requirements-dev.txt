# Development / CI dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These packages MUST NOT be installed into the production container
# image. The runtime Dockerfile only installs requirements.txt.

-r requirements.txt

# Test runner
pytest==8.3.3
pytest-cov==5.0.0

# Lint / format / static analysis
ruff==0.6.9

# Security scanners (run in CI; not needed at runtime)
bandit==1.7.10
pip-audit==2.7.3
